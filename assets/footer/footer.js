/** @jsx dom */

'use strict';

import {dom} from 'deku';
import {shouldUpdate} from '../utils/deku';

function render (component) {
  return <footer>That's all folks</footer>;
}

export default {render, shouldUpdate};